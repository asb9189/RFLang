
let userInputs = List[]
let maxInputs = 5
let numInputs = 0
let input = ""

while numInputs < maxInputs {
    input = input("Enter something: ")
    userInputs.add(input)
    numInputs = numInputs + 1
}

print("User entered")
print(userInputs)

print("After removing all 'a's")
userInputs.removeAll("a")
print(userInputs)