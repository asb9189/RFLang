package nodes.expressions

import nodes.interfaces.Expression
import nodes.root.Node

class Var(literal: String) : Node(), Expression {
    override fun eval() {
        TODO("Not yet implemented")
    }
}